# LogisticRegression
This Jupyter Notebook demonstrates a complete machine learning classification workflow using the Titanic dataset. It includes data preprocessing, feature engineering, encoding, scaling, and model training with Logistic Regression, Naive Bayes, and Support Vector Machine (SVM), followed by performance evaluation.
